# powerschool-cli-millburn
look at your failing grades in the terminal!

# setup
* install everything in `requirements.txt` with `pip` or `pip3`
* run `init.py`
* and then run `main.py`
* hopefully it works, idk
